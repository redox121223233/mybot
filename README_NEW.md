# ربات استیکر ساز تلگرام - نسخه Vercel

این نسخه از ربات استیکر ساز برای اجرا روی پلتفرم Vercel بهینه شده است و مشکل Event loop را حل می‌کند.

## 🔄 تغییرات اصلی نسبت به نسخه قبلی

### مشکل حل شده
- **Event loop is closed**: مشکل اصلی در محیط serverless Vercel که به دلیل استفاده از polling به جای webhook بود، حل شد

### تغییرات ساختاری
- **جا به جایی از polling به webhook**: ربات اکنون از webhook برای دریافت آپدیت‌ها استفاده می‌کند
- **اضافه شدن FastAPI**: برای مدیریت endpointهای وب‌هوک
- **بهینه‌سازی async**: مدیریت صحیح عملیات‌های غیرهمزمان
- **مدیریت خطا**: بهبود exception handling در محیط production

## 📋 نیازمندی‌ها

- Python 3.11+
- حساب تلگرام با دسترسی به @BotFather
- حساب Vercel (حساب رایگان کافی است)
- توکن ربات تلگرام

## 🚀 راه‌اندازی

### ۱. آماده‌سازی ربات تلگرام
1. به [@BotFather](https://t.me/botfather) بروید
2. ربات خود را ایجاد یا توکن آن را دریافت کنید
3. توکن را برای استفاده بعدی ذخیره کنید

### ۲. دیپلوی روی Vercel

#### روش اول: از طریق GitHub (توصیه شده)
1. کد را در ریپازیتوری GitHub خود آپلود کنید
2. به [Vercel Dashboard](https://vercel.com/dashboard) بروید
3. روی **New Project** کلیک کنید
4. ریپازیتوری GitHub خود را انتخاب کنید
5. در تنظیمات محیط (Environment Variables):
   - `BOT_TOKEN`: توکن ربات تلگرام خود را وارد کنید
6. روی **Deploy** کلیک کنید

#### روش دوم: از طریق Vercel CLI
```bash
# نصب Vercel CLI
npm install -g vercel

# لاگین به حساب Vercel
vercel login

# دیپلوی پروژه
vercel --prod
```

### ۳. تنظیم متغیرهای محیطی
در Vercel Dashboard، متغیرهای زیر را تنظیم کنید:
```
BOT_TOKEN=your_telegram_bot_token_here
```

### ۴. فعال‌سازی وب‌هوک
پس از دیپلوی، وب‌هوک به طور خودکار تنظیم می‌شود. برای اطمینان:
1. URL دیپلوی شده را کپی کنید (مثلاً: `https://your-app.vercel.app`)
2. تست کنید: `https://your-app.vercel.app/`
3. باید پیام `{"status": "bot is running"}` را ببینید

## 🔧 قابلیت‌ها

### ✅ کار می‌کند
- ساخت استیکر ساده با متن دلخواه
- ساخت استیکر پیشرفته با تنظیمات کامل
- مدیریت پک‌های استیکر کاربر
- بررسی عضویت در کانال
- مدیریت سهمیه روزانه
- پنل ادمین برای ارسال پیام همگانی
- پشتیبانی از فونت‌های فارسی و انگلیسی
- قابلیت افزودن عکس پس‌زمینه

### 🎨 ویژگی‌های استیکر ساز
- **استیکر ساده**: متن سریع با پس‌زمینه شفاف یا پیش‌فرض
- **استیکر پیشرفته**: تنظیمات کامل شامل:
  - موقعیت متن (بالا، وسط، پایین - چپ، وسط، راست)
  - رنگ متن (۸ رنگ پیش‌فرض)
  - اندازه فونت (کوچک، متوسط، بزرگ)
  - نوع استیکر (تصویری، ویدیویی)
- **پک‌های استیکر**: ایجاد و مدیریت پک‌های شخصی

## 📁 ساختار فایل‌ها

```
project/
├── app.py              # فایل اصلی ربات
├── requirements.txt    # نیازمندی‌های پایتون
├── vercel.json        # تنظیمات Vercel
├── README.md          # این مستندات
└── fonts/             # پوشه فونت‌ها (اختیاری)
    ├── Vazirmatn-Regular.ttf
    ├── Roboto-Regular.ttf
    └── ...
```

## 🛠 تنظیمات پیشرفته

### اضافه کردن فونت‌های فارسی
برای اضافه کردن فونت‌های جدید:
1. پوشه `fonts` را ایجاد کنید
2. فونت‌های `.ttf` را در آن قرار دهید
3. فونت‌ها به طور خودکار شناسایی می‌شوند

### تغییرات در کد
متغیرهای اصلی که می‌توانید تغییر دهید:
```python
# در app.py
CHANNEL_USERNAME = "@your_channel"    # کانال عضویت اجباری
SUPPORT_USERNAME = "@your_support"    # ادمین پشتیبانی
ADMIN_ID = 123456789                  # آیدی ادمین اصلی
DAILY_LIMIT = 5                       # محدودیت روزانه استفاده
```

## 🐛 عیب‌یابی

### مشکلات رایج

#### ۱. ربات پاسخ نمی‌دهد
- **علت**: وب‌هوک به درستی تنظیم نشده
- **راه‌حل**: 
  1. مطمئن شوید URL دیپلوی شده درست است
  2. متغیرهای محیطی را چک کنید
  3. لاگ‌های Vercel را بررسی کنید

#### ۲. خطای Event loop
- **علت**: استفاده از polling در محیط serverless
- **راه‌حل**: این نسخه با استفاده از webhook این مشکل را حل کرده است

#### ۳. خطاهای 500
- **علت**: مشکلات در کد یا نیازمندی‌ها
- **راه‌حل**: لاگ‌های Vercel را برای جزئیات بیشتر بررسی کنید

### تست محلی
برای تست در محیط توسعه:
```bash
# نصب نیازمندی‌ها
pip install -r requirements.txt

# اجرا محلی
python app.py
```

## 📈 مانیتورینگ

برای مانیتورینگ ربات:
1. به **Vercel Dashboard** بروید
2. پروژه خود را انتخاب کنید
3. **Logs** را برای دیدن لاگ‌های زنده بررسی کنید
4. **Analytics** را برای آمار استفاده بررسی کنید

## 🔒 امنیت

- توکن ربات در متغیرهای محیطی ذخیره می‌شود
- تمام ورودی‌های کاربر اعتبارسنجی می‌شوند
- فیلتر کلمات نامناسب فعال است
- محدودیت‌های استفاده روزانه برای جلوگیری از سوءاستفاده

## 📞 پشتیبانی

برای مشکلات و سوالات:
- تلگرام: `@onedaytoalive`
- ریپازیتوری GitHub: issues را بررسی کنید

## 🔄 آپدیت از نسخه قدیمی

برای انتقال از نسخه قدیمی:
1. فایل `bot.py` قدیمی را پاک کنید
2. فایل‌های جدید (`app.py`, `requirements.txt`, `vercel.json`) را آپلود کنید
3. متغیرهای محیطی را در Vercel تنظیم کنید
4. دیپلوی کنید

**توجه**: داده‌های کاربران (در حافظه RAM) با هر ری‌دیپلوی از بین می‌رود. برای ذخیره دائمی، نیاز به دیتابیس دارید.

---

این نسخه تمام مشکلات Event loop را حل کرده و برای محیط production Vercel بهینه شده است.