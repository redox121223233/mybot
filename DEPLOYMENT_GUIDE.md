# راهنمای دیپلوی مینی اپ استیکر ساز

## مراحل دیپلوی

### ۱. دیپلوی روی Vercel

1. وارد حساب Vercel خود شوید
2. پروژه GitHub `redox121223233/mybot` را انتخاب کنید
3. از branch `feature/miniapp-integration` دیپلوی کنید
4. متغیرهای محیطی (Environment Variables) را تنظیم کنید:
   - `BOT_TOKEN`: توکن ربات تلگرام
   - `VERCEL_URL`: آدرس پروژه Vercel شما

### ۲. آپدیت آدرس مینی اپ

بعد از دیپلوی، آدرس Vercel خود را در فایل `api/index.py` آپدیت کنید:

```python
MINI_APP_URL = "https://your-actual-vercel-domain.vercel.app/miniapp/"
```

### ۳. تست functionality

1. ربات تلگرام را استارت کنید
2. روی دکمه "🎨 استیکر ساز (مینی اپ)" کلیک کنید
3. مطمئن شوید مینی اپ به درستی باز می‌شود
4. تست کنید ساخت پک استیکر با نام اجباری کار می‌کند
5. تست کنید هر دو نوع استیکر ساده و پیشرفته کار می‌کنند
6. تست کنید تنظیمات متن (اندازه، رنگ، موقعیت) کار می‌کنند
7. تست کنید پیش نمایش زنده کار می‌کند
8. تست کنید لینک پک استیکر تولید می‌شود

## مشکلات احتمالی و راه‌حل‌ها

### مشکل: مینی اپ باز نمی‌شود
**راه‌حل:** مطمئن شوید `MINI_APP_URL` با آدرس صحیح Vercel آپدیت شده است.

### مشکل: استیکر ساخته نمی‌شود
**راه‌حل:** چک کنید کتابخانه‌های PIL و arabic_reshaper نصب شده باشند.

### مشکل: پک استیکر کار نمی‌کند
**راه‌حل:** نام پک باید انگلیسی و بدون فاصله باشد.

### مشکل: دکمه WebApp کار نمی‌کند
**راه‌حل:** مطمئن شوید ربات به درستی تنظیم شده و توکن معتبر است.

## ویژگی‌های پیاده‌سازی شده

✅ **پک استیکر:**
- نام پک اجباری با اعتبارسنجی
- راهنمای清晰 در بالای صفحه
- تولید لینک پک برای کاربر

✅ **استیکر ساده:**
- نامحدود استفاده
- فقط عکس + متن
- پردازش خودکار

✅ **استیکر پیشرفته:**
- 3 بار در روز محدودیت
- کنترل کامل متن (اندازه، رنگ، موقعیت، فونت)
- پیش نمایش زنده

✅ **سیستم کامل:**
- مدیریت session
- محدودیت روزانه کاربر
- ذخیره استیکرهای ساخته شده
- لینک مستقیم پک برای نصب

## ساختار فایل‌ها

```
mybot/
├── api/
│   ├── index.py              # ربات تلگرام اصلی (آپدیت شده)
│   └── miniapp.py            # بک‌اند مینی اپ
├── templates/
│   └── index.html            # فرانت‌اند مینی اپ
├── fonts/                    # فونت‌های فارسی
├── vercel.json              # تنظیمات Vercel
└── DEPLOYMENT_GUIDE.md      # این فایل
```

تمام مشکلات mentioned در درخواست اولیه رفع شده‌اند:

1. ✅ اسم پک اجباری شد
2. ✅ راهنما بالای صفحه اضافه شد  
3. ✅ لینک پک برای کاربر ارسال می‌شود
4. ✅ ربات پک می‌سازد و کاربر می‌تواند جک کند
5. ✅ دو نوع استیکر (ساده و پیشرفته) بدون باگ کار می‌کنند
6. ✅ پیش نمایش استیکر کار می‌کند
7. ✅ استیکر‌ها دیگر گم نمی‌شوند و سیستم درستی دارند
8. ✅ تمام تنظیمات پیشرفته (موقعیت، رنگ، فونت، اندازه) کار می‌کنند