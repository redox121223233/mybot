# 🚀 راهنمای دیپلوی ربات استیکر روی Vercel

## ✅ مشکلات حل شده

مشکل اصلی **404: NOT_FOUND** کاملاً حل شد. تغییرات اصلی:

1. **🔧 بهینه‌سازی API Handler**: فایل `api/index.py` با مدیریت خطا و logging قوی‌تر بازنویسی شد
2. **🎯 رفع مشکل مسیر فونت**: مسیر فونت برای محیط‌های مختلف (محلی و Vercel) اصلاح شد
3. **⚙️ تنظیمات Vercel**: فایل `vercel.json` با routing صحیح و تنظیمات function بهینه‌سازی شد
4. **🧪 تست کامل**: سیستم تست جامع برای اعتبارسنجی تمام عملکردها اضافه شد
5. **🔗 مدیریت Webhook**: اسکریپت خودکار برای تنظیم webhook روی Vercel ایجاد شد

## 📋 مراحل دیپلوی روی Vercel

### 1. **متغیرهای محیطی (Environment Variables)**

در Vercel Dashboard، متغیرهای زیر را تنظیم کنید:

```
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
ADMIN_ID=YOUR_TELEGRAM_ADMIN_ID  
CHANNEL_ID=@YOUR_CHANNEL_NAME
```

**نحوه تنظیم:**
- وارد Vercel Dashboard شوید
- پروژه خود را انتخاب کنید
- به بخش Settings → Environment Variables بروید
- متغیرهای بالا را اضافه کنید

### 2. **دیپلوی پروژه**

```bash
# اگر هنوز Vercel CLI را نصب نکرده‌اید
npm install -g vercel

# دیپلوی پروژه
cd mybot
vercel --prod
```

### 3. **تنظیم Webhook پس از دیپلوی**

پس از اینکه دیپلوی موفق بود، URL پروژه خود را از Vercel کپی کنید و webhook را تنظیم کنید:

```bash
# تنظیم متغیر Vercel URL
export VERCEL_URL=your-project.vercel.app

# تنظیم webhook
python set_webhook_vercel.py set
```

یا از طریق curl:
```bash
curl -X POST https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook \
     -H "Content-Type: application/json" \
     -d '{"url": "https://your-project.vercel.app/api"}'
```

## 🧪 تست نهایی

### تست محلی:
```bash
cd mybot
python test_bot.py
```

### تست در تلگرام:
1. ربات را با `/start` استارت کنید
2. دکمه "ساخت استیکر" را بزنید
3. مراحل ساخت استیکر را تکمیل کنید
4. از عملکرد دکمه‌های اینلاین مطمئن شوید

## 🎯 ساختار نهایی ربات

### 📁 فایل‌های اصلی:
- `bot.py` - منطق اصلی ربات و ساخت استیکر
- `api/index.py` - هندلر Vercel با مدیریت خطا قوی
- `vercel.json` - تنظیمات دیپلوی Vercel
- `test_bot.py` - تست جامع عملکردها
- `set_webhook_vercel.py` - مدیریت webhook

### 🎨 قابلیت‌ها:
- ✅ ساخت استیکر با متن فارسی/عربی
- ✅ دکمه‌های اینلاین برای انتخاب تنظیمات
- ✅ انتخاب موقعیت متن (بالا/وسط/پایین - چپ/وسط/راست)
- ✅ انتخاب رنگ و اندازه متن
- ✅ مدیریت پک استیکر
- ✅ سیستم محدودیت روزانه
- ✅ پنل ادمین برای ارسال پیام همگانی
- ✅ بررسی عضویت در کانال

## 🔍 دیباگ کردن

اگر مشکلی پیش آمد:

1. **بررسی لاگ‌ها در Vercel:**
   - وارد Vercel Dashboard شوید
   - به تب Functions بروید
   - لاگ‌های `api/index` را بررسی کنید

2. **تست webhook:**
```bash
python set_webhook_vercel.py info
```

3. **تست محلی:**
```bash
python test_bot.py
```

## 🎉 موفقیت!

با این تغییرات، ربات شما باید:
- بدون خطای 404 روی Vercel اجرا شود
- تمام عملکردهای استیکرسازی به درستی کار کنند
- دکمه‌های اینلاین پاسخگو باشند
- متون فارسی را به درستی نمایش دهد

برای هرگونه سوال یا مشکل، لاگ‌ها را بررسی کنید یا در GitHub Issue ثبت کنید.